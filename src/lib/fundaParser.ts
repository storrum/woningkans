// fundaParser.ts — minimal stub
export function parseFundaListing(html: string) {
  return { price: 0, address: "", city: "", yearBuilt: 0, livingArea: 0 };
}

export function extractPriceHistory(html: string) {
  return [];
}
